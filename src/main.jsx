import React from "react";
import { createRoot } from "react-dom/client";
import LoQueTeToca from "./App.jsx";
import "./styles.css";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LoQueTeToca />
  </React.StrictMode>
);
